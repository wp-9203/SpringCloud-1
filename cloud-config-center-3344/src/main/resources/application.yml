server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@github.com:wp-9203/springCloud-config.git
          search-paths:
            - springCloud-config #搜素目录

      label: master #读取分支
eureka:
  client:
    service-url:
      deaultZone: http://localhost:7001/eureka